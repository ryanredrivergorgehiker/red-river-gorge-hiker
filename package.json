{
  "name": "red-river-gorge-hiker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run prepare:routes && npm run validate && astro build",
    "preview": "astro preview",
    "validate": "tsx scripts/validate-products.ts",
    "test": "python3 -m unittest discover -s tests -p \"test*.py\" -v",
    "check": "npm run prepare:routes && astro check && npm test && npm run build",
    "prepare:routes": "node scripts/generate-route-elevation.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.5",
    "leaflet": "1.9.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
