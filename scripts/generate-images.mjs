/**
 * Image-generation contract (intentionally dependency-neutral until sources arrive).
 * Inputs must exactly match Photograph.sourceFilename and be manually confirmed watermarked.
 * Output widths: landscape/square 480,768,1200,1600; portrait 420,720,1080,1440;
 * panorama 640,960,1440,1920. Outputs: metadata-stripped AVIF + WebP.
 * Social composites: 1200x630 branded layouts; never clean full-frame reproductions.
 * A future implementation must hash every output and update src/data/assets.ts.
 */
throw new Error('Approved watermarked Website Export sources are not available; generation stopped safely.');
